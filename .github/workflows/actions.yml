name: npm run build

on:
  push:
    branches:
      - master
  pull_request:
    types: [ opened, synchronize ]

jobs:
  env:
    runs-on: ubuntu-latest
    steps:
      - name: Env
        run: env
  build:
    uses: ./.github/workflows/build.yaml
    secrets:
      MY_SECRET: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
